import { SxProps } from '@mui/system';

const appStyles: SxProps = {
  width: '70%',
  margin: '5rem auto',
}

const appTitleStyles: SxProps = {
  fontSize: '2rem',
  textAlign: 'center',
}

export { appStyles, appTitleStyles }